#include <stdio.h>
/**
 * main - A fucntion that prints \"Hello, World\" message
 * Return: 0 on Success execution
 */
int main(void)
{
	/* The printf() is used to print the message to stdout on the terminal */
	printf("Hello, World\n");
	return (0);
}
